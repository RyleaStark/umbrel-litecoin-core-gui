# Version-control noise
.git
.gitignore

# OS / editor cruft
.DS_Store
.vscode/
.idea/

# Dependency directories anywhere in the tree
**/node_modules

# Build artefacts and incremental compiler state
**/dist
**/build
**/*.tsbuildinfo

# Local data mounts – we don't need them inside the build context
data